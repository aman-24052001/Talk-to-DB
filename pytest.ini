[pytest]
markers =
    mongo_integration: tests that run against a real MongoDB server (skipped unless TALK_TO_DB_MONGO_URL is set). The main test suite uses mongomock; this marker is for the CI job with a live mongod service container.
